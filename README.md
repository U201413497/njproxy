# caddy-install
apt update && apt install -y curl wget && bash <(curl -Ls https://raw.githubusercontent.com/U201413497/njproxy/main/caddy-install.sh)
# juicity-install
apt update && apt install -y curl wget && bash <(curl -Ls https://raw.githubusercontent.com/U201413497/njproxy/main/juicity-install.sh)
# njproxy-front-install
apt update && apt install -y curl wget && bash <(curl -Ls https://raw.githubusercontent.com/U201413497/njproxy/main/njproxy-front.sh)
# njproxy-node-install
apt update && apt install -y curl wget && bash <(curl -Ls https://raw.githubusercontent.com/U201413497/njproxy/main/njproxy-node.sh)
# njproxy-back-install
apt update && apt install -y curl wget && bash <(curl -Ls https://raw.githubusercontent.com/U201413497/njproxy/main/njproxy-back.sh)
